---
import ThemeToggle from "./ThemeToggle.astro";
import { site } from "../data/site";

const pathname = Astro.url.pathname;
const isActive = (href: string) =>
  href === "/"
    ? pathname === "/"
    : href.startsWith("mailto:")
      ? false
      : pathname.startsWith(href);
---

<header
  class="fixed inset-x-0 top-0 z-50 border-b border-border/70 bg-background/70 shadow-[0_10px_38px_rgba(15,23,42,0.08)] backdrop-blur-2xl"
>
  <nav class="mx-auto flex min-h-16 max-w-[68rem] items-center justify-between px-5 sm:px-8" aria-label="Primary navigation">
    <a href="/" class="inline-flex items-center" aria-label={site.domain}>
      <img
        id="site-logo"
        src={site.logo.lightMode}
        data-light-src={site.logo.lightMode}
        data-dark-src={site.logo.darkMode}
        alt=""
        class="h-14 w-auto"
      />
    </a>

    <div class="hidden items-center gap-2 lg:flex">
      {
        site.navigation.map((item) => (
          <a
            href={item.href}
            class:list={[
              "glass-link rounded-full px-3 py-2 text-sm font-medium text-muted transition-all duration-200",
              isActive(item.href)
                ? "text-foreground"
                : "hover:text-foreground",
            ]}
            aria-current={isActive(item.href) ? "page" : undefined}
          >
            {item.label}
          </a>
        ))
      }
      <ThemeToggle />
    </div>

    <button
      id="mobile-menu-btn"
      class="glass-control fixed right-5 top-3 z-[60] flex h-10 items-center gap-2 rounded-full px-4 font-mono text-[0.7rem] uppercase tracking-[0.14em] text-foreground transition-colors hover:text-primary lg:hidden"
      aria-label="Open navigation"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <span>Menu</span>
      <svg
        class="h-4 w-4"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-width="2"
          d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
    </button>
  </nav>

  <div id="mobile-menu" class="hidden border-t border-border bg-background/80 backdrop-blur-2xl lg:hidden">
    <div class="mx-auto flex max-w-[68rem] flex-col px-5 py-5 sm:px-8">
      {
        site.navigation.map((item) => (
          <a
            href={item.href}
            class:list={[
              "glass-link rounded-full px-3 py-3 text-sm font-medium transition-all",
              isActive(item.href) ? "text-primary" : "text-muted",
            ]}
          >
            {item.label}
          </a>
        ))
      }
      <div class="pt-4">
        <ThemeToggle />
      </div>
    </div>
  </div>
</header>

<script>
  const menuButton = document.getElementById("mobile-menu-btn");
  const menu = document.getElementById("mobile-menu");

  menuButton?.addEventListener("click", () => {
    const isOpen = !menu?.classList.contains("hidden");
    menu?.classList.toggle("hidden", isOpen);
    menuButton.setAttribute("aria-expanded", String(!isOpen));
    menuButton.setAttribute(
      "aria-label",
      isOpen ? "Open navigation" : "Close navigation",
    );
  });

  const siteLogo = document.getElementById(
    "site-logo",
  ) as HTMLImageElement | null;
  const updateSiteLogo = () => {
    if (!siteLogo) return;

    const isDark = document.documentElement.classList.contains("dark");
    siteLogo.src = isDark
      ? siteLogo.dataset.darkSrc || ""
      : siteLogo.dataset.lightSrc || "";
  };

  updateSiteLogo();

  new MutationObserver(updateSiteLogo).observe(document.documentElement, {
    attributes: true,
    attributeFilter: ["class"],
  });
</script>
