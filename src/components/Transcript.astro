---
import { getCollection } from "astro:content";

type TranscriptItem = {
  section: string;
  title: string;
  institution: string;
  status: "Completed" | "In Progress";
  date?: string;
  description?: string;
  detail?: string;
  tagLabel?: string;
  tags?: string[];
  certificateImage?: string;
  certificateAlt?: string;
  order: number;
};

const rawCollections = await getCollection("transcript");
const entries = rawCollections
  .flatMap((entry) => entry.data as TranscriptItem[])
  .sort((a, b) => a.order - b.order);

const sectionOrder = [
  "Formal Degree",
  "Bootcamps & Institutions",
  "Online Courses — Udemy",
  "Online Courses — Great Learning",
];

const grouped = sectionOrder
  .map((section) => ({
    section,
    items: entries.filter((entry) => entry.section === section),
  }))
  .filter((group) => group.items.length > 0);
---

<section
  class="mx-auto max-w-site px-5 pb-24 pt-36 sm:px-8 md:pb-32 md:pt-44"
  id="transcript"
>
  <div>
    <div class="mb-10">
      <p class="mb-4 font-mono text-[0.72rem] font-medium uppercase tracking-[0.15em] text-subtle">
        Academic Transcript
      </p>
      <h1 class="mb-5 mt-4 max-w-4xl text-[clamp(2.8rem,6vw,4.8rem)] leading-[1.02] tracking-[-0.045em]">
        Qualifications, Bootcamps, and Certifications
      </h1>
      <p class="max-w-3xl text-base leading-relaxed text-muted">
        A complete in-app transcript of formal education and practical learning,
        with locally hosted certificate previews.
      </p>
    </div>

    {
      grouped.map((group) => (
        <div class="mb-12 last:mb-0">
          <h2 class="mb-6 font-display text-xl">{group.section}</h2>
          <div class="space-y-5">
            {group.items.map((item) => (
              <article class="border-b border-border py-6">
                <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3">
                  <div>
                    <h3 class="font-display text-lg leading-snug">
                      {item.title}
                    </h3>
                    <p class="text-sm text-primary">{item.institution}</p>
                  </div>
                  <div class="flex flex-col items-start gap-1 sm:items-end">
                    <span
                      class:list={[
                        "text-[11px] uppercase tracking-[0.12em] rounded-full px-3 py-1 border",
                        item.status === "In Progress"
                          ? "border-primary text-primary"
                          : "border-border text-muted",
                      ]}
                    >
                      {item.status}
                    </span>
                    {item.date && (
                      <p class="whitespace-nowrap font-mono text-xs text-subtle">
                        {item.date}
                      </p>
                    )}
                  </div>
                </div>

                {item.description && (
                  <p class="mt-4 text-sm leading-relaxed text-muted">
                    {item.description}
                  </p>
                )}

                {item.detail && (
                  <p class="mt-3 text-xs text-subtle">{item.detail}</p>
                )}

                {item.tags && item.tags.length > 0 && (
                  <div class="mt-4">
                    {item.tagLabel && (
                      <p class="mb-2 font-mono text-[11px] uppercase tracking-[0.12em] text-subtle">
                        {item.tagLabel}
                      </p>
                    )}
                    <div class="flex flex-wrap gap-2">
                      {item.tags.map((tag) => (
                        <span class="border border-border bg-surface px-2.5 py-1 font-mono text-xs text-muted">
                          {tag}
                        </span>
                      ))}
                    </div>
                  </div>
                )}

                {item.certificateImage && (
                  <button
                    type="button"
                    class="mt-5 inline-flex items-center gap-2 font-mono text-xs text-muted transition-colors hover:text-foreground"
                    data-cert-image={item.certificateImage}
                    data-cert-alt={item.certificateAlt ?? item.title}
                  >
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      width="16"
                      height="16"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      aria-hidden="true"
                    >
                      <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8" />
                      <circle cx="12" cy="12" r="3" />
                    </svg>
                    View Certificate
                  </button>
                )}
              </article>
            ))}
          </div>
        </div>
      ))
    }
  </div>
</section>

<div
  id="cert-modal"
  class="fixed inset-0 z-[70] hidden items-center justify-center p-4 bg-black/55"
  role="dialog"
  aria-modal="true"
  aria-label="Certificate preview"
>
  <div
    class="relative w-full max-w-4xl border border-border bg-background p-3 md:p-4"
  >
    <button
      id="cert-modal-close"
      type="button"
      class="absolute right-3 top-3 h-8 w-8 rounded-full border border-border text-muted transition-colors hover:text-foreground"
      aria-label="Close certificate preview"
    >
      <span aria-hidden="true">×</span>
    </button>
    <img
      id="cert-modal-image"
      src=""
      alt=""
      class="w-full max-h-[80vh] object-contain rounded"
      loading="lazy"
    />
  </div>
</div>

<script>
  const modal = document.getElementById("cert-modal");
  const modalImage = document.getElementById("cert-modal-image");
  const closeButton = document.getElementById("cert-modal-close");

  function openModal(src, alt) {
    if (!modal || !modalImage) return;
    modalImage.setAttribute("src", src);
    modalImage.setAttribute("alt", alt || "Certificate preview");
    modal.classList.remove("hidden");
    modal.classList.add("flex");
    document.body.style.overflow = "hidden";
  }

  function closeModal() {
    if (!modal || !modalImage) return;
    modal.classList.add("hidden");
    modal.classList.remove("flex");
    modalImage.setAttribute("src", "");
    modalImage.setAttribute("alt", "");
    document.body.style.overflow = "";
  }

  document.querySelectorAll("[data-cert-image]").forEach((button) => {
    button.addEventListener("click", () => {
      const src = button.getAttribute("data-cert-image");
      const alt = button.getAttribute("data-cert-alt");
      if (!src) return;
      openModal(src, alt || "Certificate preview");
    });
  });

  closeButton?.addEventListener("click", closeModal);

  modal?.addEventListener("click", (event) => {
    if (event.target === modal) closeModal();
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") closeModal();
  });
</script>
