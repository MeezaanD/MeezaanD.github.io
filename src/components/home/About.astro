---
import AnimatedSection from "../AnimatedSection.astro";
---

<section
  id="about"
  class="section-padding bg-foreground text-background about-section flex items-center justify-center"
>
  <div class="container-site max-w-6xl mx-auto px-6">
    <!-- Eyebrow -->
    <p
      class="about-eyebrow text-[10px] md:text-xs tracking-[0.3em] uppercase mb-6 font-semibold text-primary"
    >
      About Me
    </p>

    <!-- Main Heading -->
    <h2
      class="about-heading font-display text-4xl md:text-5xl lg:text-6xl mb-8 leading-tight"
    >
      More than just code<span class="text-primary">.</span>
    </h2>

    <!-- Content Paragraphs -->
    <div
      class="space-y-4 text-background/70 leading-relaxed text-base md:text-lg lg:text-xl max-w-3xl mb-10"
    >
      <p class="about-paragraph">
        I'm a developer who believes that great software is born at the
        intersection of technical excellence and human understanding.
      </p>
      <p class="about-paragraph" style="animation-delay: 0.1s">
        With a background spanning software engineering and web development, I
        focus on building systems that are not just functional, but delightful.
        Whether building robust backend systems or crafting user interfaces, I
        focus on creating solutions that are both powerful and intuitive.
      </p>
      <p class="about-paragraph" style="animation-delay: 0.2s">
        Beyond the screen, I'm passionate about continuous learning, exploring
        new technologies, and understanding the people behind the products we
        build.
      </p>
    </div>

    <!-- Divider -->
    <div class="about-divider mb-16">
      <div class="w-12 h-0.5 bg-primary"></div>
    </div>
  </div>
</section>

<style>
  .about-section {
    position: relative;
    overflow: hidden;
    min-height: 70vh;
  }

  .about-section::before {
    content: "";
    position: absolute;
    top: 0;
    left: -50%;
    width: 200%;
    height: 100%;
    background: radial-gradient(
      ellipse at center,
      rgba(0, 20, 137, 0.1) 0%,
      transparent 70%
    );
    animation: pulse 8s ease-in-out infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.3;
    }
    50% {
      transform: scale(1.1);
      opacity: 0.5;
    }
  }

  /* Staggered entrance animations */
  .about-eyebrow {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.6s cubic-bezier(0.25, 0.1, 0.25, 1) 0.2s forwards;
  }

  .about-heading {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.6s cubic-bezier(0.25, 0.1, 0.25, 1) 0.3s forwards;
  }

  .about-paragraph {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.6s cubic-bezier(0.25, 0.1, 0.25, 1) 0.4s forwards;
  }

  .about-divider {
    opacity: 0;
    animation: fadeIn 0.6s ease 0.5s forwards;
  }

  .about-divider > div {
    transform: scaleX(0);
    animation: scaleInX 0.8s cubic-bezier(0.25, 0.1, 0.25, 1) 0.6s forwards;
  }

  /* Keyframe Animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes scaleInX {
    from {
      transform: scaleX(0);
    }
    to {
      transform: scaleX(1);
    }
  }
</style>
