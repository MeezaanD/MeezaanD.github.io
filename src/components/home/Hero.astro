---

---

<section class="min-h-screen flex items-center justify-center pt-24">
  <div class="container-site max-w-6xl mx-auto px-6">
    <!-- Eyebrow -->
    <p
      class="hero-eyebrow text-[10px] md:text-xs tracking-[0.3em] uppercase mb-6 font-semibold"
      style="color: #001489;"
    >
      Meezaan Davids
    </p>

    <!-- Main Heading -->
    <h1 class="hero-heading font-display text-4xl md:text-5xl lg:text-6xl mb-8 leading-tight">
      Building software that <span class="italic" style="color: #001489;">matters</span><span style="color: #001489;">.</span>
    </h1>

    <!-- Philosophy Statement -->
    <p
      class="hero-description text-foreground/60 text-base md:text-lg lg:text-xl max-w-3xl mb-10 leading-relaxed"
    >
      I believe in crafting digital experiences with intention — where every
      line of code serves a purpose, and every design decision tells a story.
      Not just building apps, but solving problems that matter.
    </p>

    <!-- Divider -->
    <div class="hero-divider mb-16">
      <div class="w-12 h-0.5" style="background-color: #001489;"></div>
    </div>
  </div>
</section>

<style>
  /* Staggered entrance animations */
  .hero-eyebrow {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.6s cubic-bezier(0.25, 0.1, 0.25, 1) 0.2s forwards;
  }

  .hero-heading {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.6s cubic-bezier(0.25, 0.1, 0.25, 1) 0.3s forwards;
  }

  .hero-description {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.6s cubic-bezier(0.25, 0.1, 0.25, 1) 0.4s forwards;
  }

  .hero-divider {
    opacity: 0;
    animation: fadeIn 0.6s ease 0.5s forwards;
  }

  .hero-divider > div {
    transform: scaleX(0);
    animation: scaleInX 0.8s cubic-bezier(0.25, 0.1, 0.25, 1) 0.6s forwards;
  }

  /* Keyframe Animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes scaleInX {
    from {
      transform: scaleX(0);
    }
    to {
      transform: scaleX(1);
    }
  }
</style>